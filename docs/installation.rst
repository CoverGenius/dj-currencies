============
Installation
============

At the command line::

    $ easy_install dj-currencies

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-currencies
    $ pip install dj-currencies
